# VideoLaboratory Telegram Bot

Telegram бот для приёма предложений и сообщений об ошибках для VideoLab.

## Установка

1. Установите зависимости:
```bash
pip install -r requirements.txt
```

2. Настройте конфигурацию в `config.py`:
   - `BOT_TOKEN` - токен бота
   - `ADMIN_USERNAME` - username админа (без @)
   - `WEBSITE_URL` - ссылка на сайт
   - `INSTRUCTIONS_PATH` - путь к папке с видеоинструкциями

3. Создайте папку `Instruction` в папке `TG Bot` и добавьте видеоинструкции:
   - `video.mp4` - для категории Видео
   - `audio.mp4` - для категории Аудио
   - `images.mp4` - для категории Картинки
   - `fonts.mp4` - для категории Шрифты

4. Запустите бота:
```bash
python3 main.py
```

## Функционал

### Для пользователей:
- 📦 Предложить вещь в пак (с выбором категории и отправкой файла)
- 🐛 Сообщить об ошибке (текст + скриншот)
- 🌐 Перейти на сайт

### Для админа:
- Управление приёмом предложений (открыть/закрыть)
- Получение всех сообщений от пользователей
- Статистика

## Ещё

Внимание! Инструкции не подключены.



